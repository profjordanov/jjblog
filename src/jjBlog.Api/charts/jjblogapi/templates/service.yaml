apiVersion: v1
kind: Service
metadata:
  name: {{ template "jjblogapi.fullname" . }}
  labels:
    app: {{ template "jjblogapi.name" . }}
    chart: {{ template "jjblogapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "jjblogapi.name" . }}
    release: {{ .Release.Name }}
